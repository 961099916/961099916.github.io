# 远程调用

在微服务中不可避免的进行调用其他服务，如果单纯的采用 http 进行访问的话，IP 可能进行修改了，导致请求的 url 也需要改变，这就是的修改很复杂，而一般情况微服务的部署不会单单部署一个副本，这也就需要进行请求的负载。而 SpringCloud 提供了 feign 和 Ribbon 组建，能够进行服务间调用，支持负载均衡和权重的设置。

## Feign

## restTemplate
