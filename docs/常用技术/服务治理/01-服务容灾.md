# 服务容灾

Hystrix：Hystrix 的目标就是能够在 1 个或多个依赖出现问题时，系统依然可以稳定的运行，其手段包括隔离、限流和降级有等。

## 情景

- 当远程调用其他服务时，出现调用失败、超时、异常、服务崩溃等的原因时，不能因为被调用服务的问题，而导致其他服务的不可用，从而导致整个系统的不可用，所以可采用Hystrix进行容错。
- 当线程在一定时间能请求次数过多，占用过得资源时，可通过Hystrix进行限流。
- 防止系统的雪灾效应
