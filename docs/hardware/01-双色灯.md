# 双色灯案例

[[toc]]

## 1.Python 源码

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
import RPi.GPIO as GPIO
import time

colors = [0xFF00, 0x00FF, 0x0FF0, 0xF00F]
makerobo_pins = (11, 12)  # PIN管脚字典

GPIO.setmode(GPIO.BOARD)     # 采用实际的物理管脚给GPIO口
GPIO.setwarnings(False)      # 去除GPIO口警告
GPIO.setup(makerobo_pins, GPIO.OUT)   # 设置Pin模式为输出模式
GPIO.output(makerobo_pins, GPIO.LOW)  # 设置Pin管脚为低电平(0V)关闭LED

p_R = GPIO.PWM(makerobo_pins[0], 2000)  # 设置频率为2KHz
p_G = GPIO.PWM(makerobo_pins[1], 2000)  # 设置频率为2KHz

# 初始化占空比为0(led关闭)
p_R.start(0)
p_G.start(0)

def makerobo_pwm_map(x, in_min, in_max, out_min, out_max):
	return (x - in_min) * (out_max - out_min) / (in_max - in_min) + out_min

def makerobo_set_Color(col):   # 例如:col = 0x1122
	R_val = col  >> 8
	G_val = col & 0x00FF
	# 把0-255的范围同比例缩小到0-100之间
	R_val = makerobo_pwm_map(R_val, 0, 255, 0, 100)
	G_val = makerobo_pwm_map(G_val, 0, 255, 0, 100)

	p_R.ChangeDutyCycle(R_val)     # 改变占空比
	p_G.ChangeDutyCycle(G_val)     # 改变占空比

# 调用循环函数
def makerobo_loop():
	while True:
		for col in colors:
			makerobo_set_Color(col)
			time.sleep(0.5)
# 释放资源
def makerobo_destroy():
	p_G.stop()
	p_R.stop()
	GPIO.output(makerobo_pins, GPIO.LOW)    # 关闭所有LED
	GPIO.cleanup()                          # 释放资源

# 程序入口
if __name__ == "__main__":
	try:
		makerobo_loop()       # 调用循环函数
	except KeyboardInterrupt:  # 当按下Ctrl+C时，将执行destroy()子程序。
		makerobo_destroy()    # 释放资源

```
## 2.源码解读

### 2.1 参数的解析

```python
colors = [0xFF00, 0x00FF, 0x0FF0, 0xF00F]
makerobo_pins = (11, 12)  # PIN管脚字典

GPIO.setmode(GPIO.BOARD)     # 采用实际的物理管脚给GPIO口
GPIO.setwarnings(False)      # 去除GPIO口警告
GPIO.setup(makerobo_pins, GPIO.OUT)   # 设置Pin模式为输出模式
GPIO.output(makerobo_pins, GPIO.LOW)  # 设置Pin管脚为低电平(0V)关闭LED

p_R = GPIO.PWM(makerobo_pins[0], 2000)  # 设置频率为2KHz
p_G = GPIO.PWM(makerobo_pins[1], 2000)  # 设置频率为2KHz

# 初始化占空比为0(led关闭)
p_R.start(0)
p_G.start(0)
```

双色灯，有两个输出引脚，输出的范围是0~100，而颜色的十六禁止00～FF，可通过给引脚传入0~100从而控制灯颜色的深浅，`colors`：包含两个引脚颜色的数据，`makerobo_pins`：设置引脚，可通过树莓派的原理图得出。整个系统需要先设置实际的物理管脚给GPIO口，然后取出GPIO口的警告，然后再设置引脚是输入还是输出，然后设置默认引脚是低电平还是高电平。然后设置引脚的占空比，从而设置灯的亮度。初始化的占空比为0，让LED灯关闭。

### 2.2 让颜色适配输出的范围

```python
def makerobo_pwm_map(x, in_min, in_max, out_min, out_max):
	return (x - in_min) * (out_max - out_min) / (in_max - in_min) + out_min
```

简单的算法实现0~255转换成0~100, （当前值-输入的最小值）/ （输入的最大值-输入的最小值）再乘以（输出范围的最大值-输出范围的最小值），然后再加上输出的最小值。

### 2.3 设置颜色
```python
def makerobo_set_Color(col):   # 例如:col = 0x1122
	R_val = col  >> 8
	G_val = col & 0x00FF
	# 把0-255的范围同比例缩小到0-100之间
	R_val = makerobo_pwm_map(R_val, 0, 255, 0, 100)
	G_val = makerobo_pwm_map(G_val, 0, 255, 0, 100)

	p_R.ChangeDutyCycle(R_val)     # 改变占空比
	p_G.ChangeDutyCycle(G_val)     # 改变占空比
```

通过col右移8位从而取得前两位，然后col与上0x00FF实现取得后两位，然后缩小范围转换成要输出的值，然后设置值即可。

### 2.4 其他

其他的就是捕获键盘中断异常，异常是进行设置的初始化，无异常时进行循环设置颜色
